# Docker Compose override for CPU-only systems (without NVIDIA GPU)
# Usage: docker-compose -f docker-compose.yml -f docker-compose.cpu.yml up -d

services:
  ollama:
    deploy: {}  # Override to remove GPU configuration
